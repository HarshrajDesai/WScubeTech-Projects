import 'package:flutter/material.dart';

class DairyOwnerProfile extends StatelessWidget {
  const DairyOwnerProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}